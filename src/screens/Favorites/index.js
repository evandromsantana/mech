import React from 'react';
import { text } from 'react-native';
import { Container } from './styles';

export default () => {
    return (
        <Container>
            <Text>Favorites</Text>
        </Container>
    );
}